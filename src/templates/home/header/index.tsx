'use client';
import * as S from './styles';
import { FC } from 'react';
import { HeaderC as C } from './constants';

export const Header: FC = () => {
  return (
    <S.Header id={C.id}>
 
    </S.Header>
  );
};
